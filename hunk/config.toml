# Hunk defaults optimized for interactive/agent-assisted reviews.
# https://github.com/modem-dev/hunk/blob/main/docs/agent-workflows.md

mode = "auto"
line_numbers = true
wrap_lines = false
agent_notes = true
exclude_untracked = false
