csw ports forward 2222:2222 &
# Save the process ID of the last command
pid=$!

# Wait for the first command to complete before running the second command
wait $pid

# Forward the socket
ssh -R /home/%r/.local/run/lawn/server-$(mktemp -u XXXXXX).sock:$HOME/.local/run/lawn/server-0.sock codespace


