#!/bin/zsh

# Log all output
exec > >(tee -i "$HOME/dotfiles_install.log")
exec 2>&1
set -x

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

if [[ -n "$CODESPACES" ]]; then
    "$SCRIPT_DIR/install-codespaces"
else
    "$SCRIPT_DIR/install-local"
fi
